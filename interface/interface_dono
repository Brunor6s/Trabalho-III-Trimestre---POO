"""
Módulo interface_dono - Interface específica para o usuário Dono.

Implementa todas as funcionalidades disponíveis para o dono do hotel.
"""

import tkinter as tk
from .base_interface import BaseInterface
from .componentes import (FormularioBase, TabelaModerna, criar_titulo_pagina,
                          mostrar_mensagem_sucesso, mostrar_mensagem_erro, confirmar_acao)
from . import cores


class InterfaceDono(BaseInterface):
    """Interface completa para o Dono do hotel."""
    
    def __init__(self, janela, usuario, services, on_logout):
        """
        Inicializa a interface do dono.
        
        Args:
            janela: Janela principal
            usuario: Dados do usuário logado
            services: Dicionário com todos os services
            on_logout: Callback de logout
        """
        self.services = services
        super().__init__(janela, usuario, on_logout)
        
        # Criar botões do menu
        self._criar_menu()
        
        # Mostrar dashboard inicial
        self.mostrar_dashboard()
    
    def _criar_menu(self):
        """Cria os botões de navegação do menu."""
        self.adicionar_botao_menu("Dashboard", "📊", self.mostrar_dashboard)
        self.adicionar_botao_menu("Cadastrar Cliente", "👤", self.mostrar_cadastro_cliente)
        self.adicionar_botao_menu("Cadastrar Funcionário", "👨‍💼", self.mostrar_cadastro_funcionario)
        self.adicionar_botao_menu("Cadastrar Quarto", "🛏️", self.mostrar_cadastro_quarto)
        self.adicionar_botao_menu("Listar Clientes", "📋", self.mostrar_lista_clientes)
        self.adicionar_botao_menu("Listar Funcionários", "📋", self.mostrar_lista_funcionarios)
        self.adicionar_botao_menu("Listar Quartos", "📋", self.mostrar_lista_quartos)
        self.adicionar_botao_menu("Reservas", "📅", self.mostrar_reservas)
    
    def mostrar_dashboard(self):
        """Exibe o dashboard com estatísticas."""
        self.limpar_conteudo()
        criar_titulo_pagina(self.conteudo_frame, "Dashboard", "Visão geral do sistema")
        
        # Container para cards
        cards_frame = tk.Frame(self.conteudo_frame, bg=cores.FUNDO_CLARO)
        cards_frame.pack(fill='both', expand=True, padx=20, pady=20)
        
        # Calcular estatísticas
        total_clientes = len(self.services['cliente'].clientes)
        total_funcionarios = len(self.services['funcionario'].funcionarios)
        total_quartos = len(self.services['quarto'].quartos)
        quartos_disponiveis = len([q for q in self.services['quarto'].quartos if q.disponivel])
        total_reservas = len(self.services['reserva'].reservas)
        
        # Criar cards
        self._criar_card_stat(cards_frame, "👥 Clientes", str(total_clientes), cores.DESTAQUE, 0, 0)
        self._criar_card_stat(cards_frame, "👨‍💼 Funcionários", str(total_funcionarios), cores.SUCESSO, 0, 1)
        self._criar_card_stat(cards_frame, "🛏️ Quartos", str(total_quartos), cores.AVISO, 1, 0)
        self._criar_card_stat(cards_frame, "✅ Disponíveis", str(quartos_disponiveis), cores.SUCESSO, 1, 1)
        self._criar_card_stat(cards_frame, "📅 Reservas", str(total_reservas), cores.PRIMARIA, 2, 0)
    
    def _criar_card_stat(self, parent, titulo, valor, cor, row, col):
        """Cria um card de estatística."""
        card = tk.Frame(parent, bg='white', relief='solid', borderwidth=1)
        card.grid(row=row, column=col, padx=10, pady=10, sticky='nsew')
        
        # Configurar grid
        parent.grid_rowconfigure(row, weight=1)
        parent.grid_columnconfigure(col, weight=1)
        
        # Conteúdo
        tk.Label(card,
                text=titulo,
                font=('Segoe UI', 12),
                bg='white',
                fg=cores.TEXTO_SECUNDARIO).pack(pady=(20, 5))
        
        tk.Label(card,
                text=valor,
                font=('Segoe UI', 32, 'bold'),
                bg='white',
                fg=cor).pack(pady=(5, 20))
    
    def mostrar_cadastro_cliente(self):
        """Exibe formulário de cadastro de cliente."""
        self.limpar_conteudo()
        criar_titulo_pagina(self.conteudo_frame, "Cadastrar Cliente",
                          "Adicione um novo cliente ao sistema")
        
        # Criar formulário
        form = FormularioBase(self.conteudo_frame)
        form.adicionar_campo('nome', 'Nome Completo')
        form.adicionar_campo('documento', 'CPF (apenas números)')
        form.adicionar_campo('email', 'Email')
        form.adicionar_campo('telefone', 'Telefone')
        form.adicionar_campo('senha', 'Senha', tipo='password')
        
        def salvar():
            valores = form.obter_valores()
            try:
                cliente = self.services['cliente'].criar(
                    nome=valores['nome'],
                    documento=valores['documento'],
                    email=valores['email'],
                    senha=valores['senha'],
                    telefone=valores['telefone'],
                    autor='dono'
                )
                mostrar_mensagem_sucesso(f"Cliente {cliente.nome} cadastrado com sucesso!")
                form.limpar()
            except Exception as e:
                mostrar_mensagem_erro(str(e))
        
        form.adicionar_botoes([
            ("Salvar", salvar, 'success'),
            ("Limpar", form.limpar, 'normal')
        ])
    
    def mostrar_cadastro_funcionario(self):
        """Exibe formulário de cadastro de funcionário."""
        self.limpar_conteudo()
        criar_titulo_pagina(self.conteudo_frame, "Cadastrar Funcionário",
                          "Adicione um novo funcionário ao sistema")
        
        form = FormularioBase(self.conteudo_frame)
        form.adicionar_campo('nome', 'Nome Completo')
        form.adicionar_campo('documento', 'CPF (apenas números)')
        form.adicionar_campo('email', 'Email')
        form.adicionar_campo('cargo', 'Cargo', tipo='combo',
                           opcoes=['Recepcionista', 'Gerente', 'Limpeza', 'Manutenção'])
        form.adicionar_campo('senha', 'Senha', tipo='password')
        
        def salvar():
            valores = form.obter_valores()
            try:
                func = self.services['funcionario'].criar(
                    nome=valores['nome'],
                    email=valores['email'],
                    senha=valores['senha'],
                    documento=valores['documento'],
                    cargo=valores['cargo'],
                    autor='dono'
                )
                mostrar_mensagem_sucesso(f"Funcionário {func.nome} cadastrado com sucesso!")
                form.limpar()
            except Exception as e:
                mostrar_mensagem_erro(str(e))
        
        form.adicionar_botoes([
            ("Salvar", salvar, 'success'),
            ("Limpar", form.limpar, 'normal')
        ])
    
    def mostrar_cadastro_quarto(self):
        """Exibe formulário de cadastro de quarto."""
        self.limpar_conteudo()
        criar_titulo_pagina(self.conteudo_frame, "Cadastrar Quarto",
                          "Adicione um novo quarto ao sistema")
        
        form = FormularioBase(self.conteudo_frame)
        form.adicionar_campo('numero', 'Número do Quarto')
        form.adicionar_campo('tipo', 'Tipo', tipo='combo',
                           opcoes=['Solteiro', 'Casal', 'Luxo', 'Suite', 'Presidencial'])
        form.adicionar_campo('preco', 'Preço da Diária (R$)')
        
        def salvar():
            valores = form.obter_valores()
            try:
                quarto = self.services['quarto'].criar(
                    numero=valores['numero'],
                    preco=valores['preco'],
                    tipo=valores['tipo']
                )
                mostrar_mensagem_sucesso(f"Quarto {quarto.numero} cadastrado com sucesso!")
                form.limpar()
            except Exception as e:
                mostrar_mensagem_erro(str(e))
        
        form.adicionar_botoes([
            ("Salvar", salvar, 'success'),
            ("Limpar", form.limpar, 'normal')
        ])
    
    def mostrar_lista_clientes(self):
        """Exibe lista de clientes."""
        self.limpar_conteudo()
        criar_titulo_pagina(self.conteudo_frame, "Clientes Cadastrados",
                          f"Total: {len(self.services['cliente'].clientes)}")
        
        # Criar tabela
        colunas = [
            ('id', 'ID'),
            ('nome', 'Nome'),
            ('cpf', 'CPF'),
            ('email', 'Email'),
            ('telefone', 'Telefone')
        ]
        tabela = TabelaModerna(self.conteudo_frame, colunas,
                              {'id': 50, 'nome': 200, 'cpf': 120, 'email': 200, 'telefone': 120})
        
        # Preencher tabela
        for cliente in self.services['cliente'].clientes:
            tabela.adicionar_item((
                cliente.idCliente,
                cliente.nome,
                cliente.documento,
                cliente.email,
                cliente.telefone
            ))
        
        # Botões de ação
        btn_frame = tk.Frame(self.conteudo_frame, bg=cores.FUNDO_CLARO)
        btn_frame.pack(fill='x', padx=20, pady=10)
        
        def editar_cliente():
            selecionado = tabela.obter_selecionado()
            if not selecionado:
                mostrar_mensagem_erro("Selecione um cliente na tabela.")
                return
            
            id_cliente = selecionado[0]
            self._mostrar_form_editar_cliente(id_cliente)
        
        def excluir_cliente():
            selecionado = tabela.obter_selecionado()
            if not selecionado:
                mostrar_mensagem_erro("Selecione um cliente na tabela.")
                return
            
            if confirmar_acao(f"Deseja realmente excluir o cliente {selecionado[1]}?"):
                try:
                    msg = self.services['cliente'].excluir(selecionado[0], autor='dono')
                    mostrar_mensagem_sucesso(msg)
                    self.mostrar_lista_clientes()
                except Exception as e:
                    mostrar_mensagem_erro(str(e))
        
        tk.Button(btn_frame,
                 text="✏️ Editar",
                 font=('Segoe UI', 10, 'bold'),
                 bg=cores.AVISO,
                 fg=cores.TEXTO_CLARO,
                 activebackground='#D68910',
                 relief='flat',
                 cursor='hand2',
                 command=editar_cliente).pack(side='left', padx=5, ipady=8, ipadx=15)
        
        tk.Button(btn_frame,
                 text="🗑️ Excluir",
                 font=('Segoe UI', 10, 'bold'),
                 bg=cores.ERRO,
                 fg=cores.TEXTO_CLARO,
                 activebackground='#C0392B',
                 relief='flat',
                 cursor='hand2',
                 command=excluir_cliente).pack(side='left', padx=5, ipady=8, ipadx=15)
    
    def _mostrar_form_editar_cliente(self, id_cliente):
        """Exibe formulário para editar cliente."""
        cliente = self.services['cliente'].buscarPorId(id_cliente)
        if not cliente:
            mostrar_mensagem_erro("Cliente não encontrado.")
            return
        
        self.limpar_conteudo()
        criar_titulo_pagina(self.conteudo_frame, "Editar Cliente",
                          f"Editando: {cliente.nome}")
        
        form = FormularioBase(self.conteudo_frame)
        form.adicionar_campo('nome', 'Nome Completo')
        form.adicionar_campo('email', 'Email')
        form.adicionar_campo('telefone', 'Telefone')
        form.adicionar_campo('senha', 'Nova Senha (deixe vazio para manter)', tipo='password')
        
        # Preencher com dados atuais
        form.campos['nome'].insert(0, cliente.nome)
        form.campos['email'].insert(0, cliente.email)
        form.campos['telefone'].insert(0, cliente.telefone)
        
        def salvar():
            valores = form.obter_valores()
            try:
                self.services['cliente'].editar(
                    id_cliente=id_cliente,
                    nome=valores['nome'] if valores['nome'] else None,
                    email=valores['email'] if valores['email'] else None,
                    telefone=valores['telefone'] if valores['telefone'] else None,
                    senha=valores['senha'] if valores['senha'] else None,
                    autor='dono'
                )
                mostrar_mensagem_sucesso("Cliente editado com sucesso!")
                self.mostrar_lista_clientes()
            except Exception as e:
                mostrar_mensagem_erro(str(e))
        
        form.adicionar_botoes([
            ("Salvar", salvar, 'success'),
            ("Cancelar", self.mostrar_lista_clientes, 'normal')
        ])
    
    def mostrar_lista_funcionarios(self):
        """Exibe lista de funcionários."""
        self.limpar_conteudo()
        criar_titulo_pagina(self.conteudo_frame, "Funcionários Cadastrados",
                          f"Total: {len(self.services['funcionario'].funcionarios)}")
        
        colunas = [
            ('id', 'ID'),
            ('nome', 'Nome'),
            ('cargo', 'Cargo'),
            ('cpf', 'CPF'),
            ('email', 'Email')
        ]
        tabela = TabelaModerna(self.conteudo_frame, colunas,
                              {'id': 50, 'nome': 200, 'cargo': 150, 'cpf': 120, 'email': 200})
        
        for func in self.services['funcionario'].funcionarios:
            tabela.adicionar_item((
                func.idFuncionario,
                func.nome,
                func.cargo,
                func.documento,
                func.email
            ))
        
        # Botões de ação
        btn_frame = tk.Frame(self.conteudo_frame, bg=cores.FUNDO_CLARO)
        btn_frame.pack(fill='x', padx=20, pady=10)
        
        def editar_funcionario():
            selecionado = tabela.obter_selecionado()
            if not selecionado:
                mostrar_mensagem_erro("Selecione um funcionário na tabela.")
                return
            
            id_funcionario = selecionado[0]
            self._mostrar_form_editar_funcionario(id_funcionario)
        
        def excluir_funcionario():
            selecionado = tabela.obter_selecionado()
            if not selecionado:
                mostrar_mensagem_erro("Selecione um funcionário na tabela.")
                return
            
            if confirmar_acao(f"Deseja realmente excluir o funcionário {selecionado[1]}?"):
                try:
                    msg = self.services['funcionario'].excluir(selecionado[0], autor='dono')
                    mostrar_mensagem_sucesso(msg)
                    self.mostrar_lista_funcionarios()
                except Exception as e:
                    mostrar_mensagem_erro(str(e))
        
        tk.Button(btn_frame,
                 text="✏️ Editar",
                 font=('Segoe UI', 10, 'bold'),
                 bg=cores.AVISO,
                 fg=cores.TEXTO_CLARO,
                 activebackground='#D68910',
                 relief='flat',
                 cursor='hand2',
                 command=editar_funcionario).pack(side='left', padx=5, ipady=8, ipadx=15)
        
        tk.Button(btn_frame,
                 text="🗑️ Excluir",
                 font=('Segoe UI', 10, 'bold'),
                 bg=cores.ERRO,
                 fg=cores.TEXTO_CLARO,
                 activebackground='#C0392B',
                 relief='flat',
                 cursor='hand2',
                 command=excluir_funcionario).pack(side='left', padx=5, ipady=8, ipadx=15)
    
    def _mostrar_form_editar_funcionario(self, id_funcionario):
        """Exibe formulário para editar funcionário."""
        funcionario = self.services['funcionario'].buscarPorId(id_funcionario)
        if not funcionario:
            mostrar_mensagem_erro("Funcionário não encontrado.")
            return
        
        self.limpar_conteudo()
        criar_titulo_pagina(self.conteudo_frame, "Editar Funcionário",
                          f"Editando: {funcionario.nome}")
        
        form = FormularioBase(self.conteudo_frame)
        form.adicionar_campo('nome', 'Nome Completo')
        form.adicionar_campo('email', 'Email')
        form.adicionar_campo('cargo', 'Cargo', tipo='combo',
                           opcoes=['Recepcionista', 'Gerente', 'Limpeza', 'Manutenção'])
        form.adicionar_campo('senha', 'Nova Senha (deixe vazio para manter)', tipo='password')
        
        # Preencher com dados atuais
        form.campos['nome'].insert(0, funcionario.nome)
        form.campos['email'].insert(0, funcionario.email)
        form.campos['cargo'].set(funcionario.cargo)
        
        def salvar():
            valores = form.obter_valores()
            try:
                self.services['funcionario'].editar(
                    id_funcionario=id_funcionario,
                    nome=valores['nome'] if valores['nome'] else None,
                    email=valores['email'] if valores['email'] else None,
                    cargo=valores['cargo'] if valores['cargo'] else None,
                    senha=valores['senha'] if valores['senha'] else None,
                    autor='dono'
                )
                mostrar_mensagem_sucesso("Funcionário editado com sucesso!")
                self.mostrar_lista_funcionarios()
            except Exception as e:
                mostrar_mensagem_erro(str(e))
        
        form.adicionar_botoes([
            ("Salvar", salvar, 'success'),
            ("Cancelar", self.mostrar_lista_funcionarios, 'normal')
        ])
    
    def mostrar_lista_quartos(self):
        """Exibe lista de quartos."""
        self.limpar_conteudo()
        criar_titulo_pagina(self.conteudo_frame, "Quartos Cadastrados",
                          f"Total: {len(self.services['quarto'].quartos)}")
        
        colunas = [
            ('numero', 'Número'),
            ('tipo', 'Tipo'),
            ('preco', 'Preço Diária'),
            ('status', 'Status')
        ]
        tabela = TabelaModerna(self.conteudo_frame, colunas,
                              {'numero': 100, 'tipo': 150, 'preco': 150, 'status': 120})
        
        for quarto in self.services['quarto'].quartos:
            status = "✅ Disponível" if quarto.disponivel else "❌ Ocupado"
            tabela.adicionar_item((
                quarto.numero,
                quarto.tipo,
                f"R$ {quarto.precoDiaria:.2f}",
                status
            ))
    
    def mostrar_reservas(self):
        """Exibe lista de reservas."""
        self.limpar_conteudo()
        criar_titulo_pagina(self.conteudo_frame, "Reservas",
                          f"Total: {len(self.services['reserva'].reservas)}")
        
        colunas = [
            ('id', 'ID'),
            ('cliente', 'Cliente'),
            ('quarto', 'Quarto'),
            ('checkin', 'Check-in'),
            ('checkout', 'Check-out'),
            ('valor', 'Valor Total')
        ]
        tabela = TabelaModerna(self.conteudo_frame, colunas,
                              {'id': 50, 'cliente': 150, 'quarto': 80,
                               'checkin': 100, 'checkout': 100, 'valor': 100})
        
        for reserva in self.services['reserva'].reservas:
            tabela.adicionar_item((
                reserva.idReserva,
                reserva.cliente.nome,
                reserva.quarto.numero,
                reserva.dataCheckin.strftime('%d/%m/%Y'),
                reserva.dataCheckout.strftime('%d/%m/%Y'),
                f"R$ {reserva.valorTotal:.2f}"
            ))
